echo "🧪 Running unit and application tests before push..."
echo "(Integration tests run in CI)"

# Run tests excluding integration tests that require Docker
pnpm test -- --exclude "**/routes/**"

echo "✅ All unit tests passed! Pushing to remote..."
