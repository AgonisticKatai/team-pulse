echo "🔍 Running pre-commit checks..."

# Run lint-staged (lint and format only changed files)
pnpm lint-staged

# Run type-check on all packages
echo "🔎 Type checking..."
pnpm type-check

echo "✅ Pre-commit checks passed!"
