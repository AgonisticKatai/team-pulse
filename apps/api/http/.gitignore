# Ignore custom HTTP files with sensitive data
*-custom.http
*-local.http
*-tokens.http
*-credentials.http

# REST Client response cache (if enabled)
.httpCache/

# Note: Environment variables are now configured in ../../.vscode/settings.json
# For personal overrides, use ../../.vscode/settings.local.json (already in gitignore)
