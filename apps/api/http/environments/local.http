###############################################################################
# Local Development Environment
#
# Variables for testing the API locally.
# REST Client will automatically use these variables in all .http files.
###############################################################################

### Base URL for local development
@baseUrl = http://localhost:3000

### Test users (created by seed script or manually)
# USER role
@userEmail = user@test.com
@userPassword = UserPassword123!

# ADMIN role
@adminEmail = admin@test.com
@adminPassword = AdminPassword123!

# SUPER_ADMIN role
@superAdminEmail = superadmin@test.com
@superAdminPassword = SuperAdminPassword123!

###############################################################################
# Session tokens (auto-populated after login)
# These will be set automatically when you run login requests
###############################################################################

# @accessToken = (set automatically after login)
# @refreshToken = (set automatically after login)
# @userId = (set automatically after login)
# @teamId = (set automatically after creating/listing teams)
