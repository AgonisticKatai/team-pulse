# API Configuration
# URL of the TeamPulse API backend
#
# Leave empty (default) to use relative URLs:
# - Development: Vite proxy redirects /api/* to http://localhost:3000/api/*
# - Production: Vercel rewrites /api/* to serverless functions
#
# Only set this if you need to point to a different API server
# VITE_API_URL=http://localhost:3000
